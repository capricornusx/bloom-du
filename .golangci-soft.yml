run:
  concurrency: 4
  timeout: 5m
  tests: true
  allow-parallel-runners: true
  go: '1.21'

linters:
  enable:
    - gosec
    - gci

issues:
  new: true
  exclude-rules:

    - linters:
        - staticcheck
      text: "SA4027:"

    - linters:
        - staticcheck
      text: "ST1005:"

